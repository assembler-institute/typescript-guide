export { Boat } from './boat';
export { CargoShip } from './cargo_ship';
export { Pokemon } from './pokemon';

// is good practice to use a barrel file to export all the interfaces from a folder
// and sort them alphabetically using the sort lines command
// (cmd + shift + p -> sort lines: Ascending)
